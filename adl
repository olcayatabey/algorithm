#include <iostream>

namespace A {
	class X {
	public:
		X(int i):data{i}{}

		void foo(){}
	private:
		int data;
		bool friend  operator==( const X &lhs ,const X &rhs)
		{
			return lhs.data == rhs.data;
		}

	};
}

int main()
{
	A::X a(41), b(25);
	if (a == b)
	//	do_something(...);
}
